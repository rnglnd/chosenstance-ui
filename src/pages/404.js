import React from 'react';
import { pageClassName } from '../utils/styles';

const NotFoundPage = () => (
  <div className={pageClassName}>
    <h2>We are so sorry</h2>
    <p>We can't find what you're looking for but please continue to look around.</p>
  </div>
)

export default NotFoundPage
